# Secrets & generated browser configs — NEVER commit real keys
.env
.env.local

# Generated from .env / CI only (not in git)
firebase-config.js
ai-secrets.js
email-config.js
recaptcha-config.js

# Examples are OK to commit (placeholders only)
!firebase-config.example.js
!email-config.example.js
!recaptcha-config.example.js
!contact-config.example.js

# Public AI settings (endpoint URL + models, no keys) may be tracked
# ai-config.js
